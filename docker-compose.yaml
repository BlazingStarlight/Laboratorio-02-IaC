version: '3'

services:
  app1:
    container_name: API_1
    build: ./api
    ports:
      - 3000:3000
    environment:
      - MESSAGE=Hola, soy la API 1

  app2:
    container_name: API_2
    build: ./api
    ports:
      - 3001:3000
    environment:
      - MESSAGE=Hola, soy la API 2

  app3:
    container_name: API_3
    build: ./api
    ports:
      - 3002:3000
    environment:
      - MESSAGE=Hola, soy la API 3